# Archive

Historical Palm code preserved during the 0.4.0 restructuring.

| Path | Contents |
|------|----------|
| `legacy-src/` | Pre-restructure `src/palm` tree (CLI, legacy wizards, old core engines) |
| `legacy-tests/` | Tests targeting the old implementation |
| `wizards/` | Example wizard definitions |
| `main.py` | Old root entry shim |

**Do not import from `archive/` in new code.** Reference only for migration.