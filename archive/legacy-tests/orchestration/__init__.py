"""
Test package for the Palm Orchestration Engine (core).

All tests in this package (except the single optional integration file)
must remain completely independent of the Behavior Tree engine and must
use only TestMode + TestBackend.
"""
