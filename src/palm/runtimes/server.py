"""
Server runtime — HTTP/WebSocket Palm host (placeholder).

Future home for daemon-facing API endpoints.
"""

from __future__ import annotations


def run_server(host: str = "127.0.0.1", port: int = 8080) -> None:
    """Start the Palm server runtime (not yet implemented)."""
    raise NotImplementedError(f"Server runtime is not implemented yet (would bind {host}:{port})")
